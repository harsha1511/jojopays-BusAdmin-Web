import axios from "axios";

const constants:any = {
    auth: {
        login:"login"
    },
    Company: {
        driver: "getDriverData",
        bus: "getBusData"

    }   
};

export default constants;
